services:

  nbxplorer:
    restart: unless-stopped
    container_name: generated_nbxplorer_1
    image: nicolasdorier/nbxplorer:2.6.11
    expose:
      - "32838"
    environment:
      NBXPLORER_NETWORK: ${NBITCOIN_NETWORK:-regtest}
      NBXPLORER_BIND: 0.0.0.0:32838
      NBXPLORER_TRIMEVENTS: 10000
      NBXPLORER_SIGNALFILESDIR: /datadir
      NBXPLORER_POSTGRES: User ID=${PSQL_UID:-foo};Host=${PSQL_HOST:-postgres};Port=5432;Application Name=nbxplorer;MaxPoolSize=20;Database=${PSQL_DBNAME:-btcpay0}nbxplorer;Password=${PSQL_PSW:-foobarbaz}
    volumes:
      - "nbxplorer_datadir:/datadir"

  btcpayserver:
    environment:
          BTCPAY_EXPLORERPOSTGRES: User ID=postgres;Host=postgres;Port=5432;Application Name=btcpayserver;MaxPoolSize=80;Database=nbxplorer${NBITCOIN_NETWORK:-regtest}
    volumes:
      - "nbxplorer_datadir:/root/.nbxplorer"

volumes:
  nbxplorer_datadir:
